spring.application.name=shop-backend

server.port=${PORT:8080}

# Database configuration
spring.datasource.username=${DB_USERNAME:sa}
spring.datasource.password=${DB_PASSWORD:221122}
spring.datasource.url=${DB_URL:jdbc:sqlserver://localhost;databaseName=ecommerce_shop;encrypt=true;trustServerCertificate=true}

#Swagger enable
springdoc.api-docs.enabled=true

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

#Mail
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:465}
spring.mail.username=${MAIL_USERNAME:linnmarn882@gmail.com}
spring.mail.password=${MAIL_PASSWORD:unhlvfeyxbfzceew}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# HTTP Email API Keys (for Render Cloud deployment)
resend.api.key=${RESEND_API_KEY:}
brevo.api.key=${BREVO_API_KEY:}

#JWT
jwt.auth.app = JasShop
jwt.auth.secret_key = ${JWT_KEY:DefaultSecretKeyForJasShopECommerceApp2026SecureKey!}
jwt.auth.expires_in = 3600




